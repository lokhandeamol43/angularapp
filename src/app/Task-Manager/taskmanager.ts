export class TaskManager {
    id: any = 0;
    task_name: any = '';
    status: string = 'Select Status'; // Default status is 'not started'
    dueDate: '';   // Initialize with an empty string
  }
  