import { UserFilterPipeAssignPipe } from './user-filter-pipe-assign.pipe';

describe('UserFilterPipeAssignPipe', () => {
  it('create an instance', () => {
    const pipe = new UserFilterPipeAssignPipe();
    expect(pipe).toBeTruthy();
  });
});
